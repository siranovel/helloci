version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say Hello"
          command: "echo hello, world!" 

workflows:
  version: 2.1
  my-workflow:
    jobs:
      - say-hello:
          filters:
            branches:
              only:
                - main
      

